= render 'layouts/main'

.container
  .row
    .tag-list
      - @tags.each do |tag|
        .col-sm-4.col-sm-3
          = link_to tag_path(tag.name), class: "tag.thumbnail" do
            .tag-name
              = tag.name
